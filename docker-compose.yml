services:
  web:
    build: .
    command: daphne -b 0.0.0.0 -p 8000 config.asgi:application
    volumes:
      - ./src:/app
    ports:
      - "8000:8000"
    
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"